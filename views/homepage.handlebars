<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Book Club Blog</title>
  <link rel="stylesheet" href="/css/jass.css">
  <link rel="stylesheet" href="/css/style.css">
</head>
 
<body>
    {{#if logged_in}}
  <div class="flex-column justify-space-around">
    <header class="display-flex justify-space-between align-center p-2">
      <h1>Book Club Blog</h1>
      <nav>
        <!-- Conditionally render login or logout links -->
        <button id="blog-post">Blog Posts</button>
        <button id="browse">Browse</button>        
        <button class="no-button" id="logout">Logout</button>
        {{else}}
        <a href="/login">Login</a>
        {{/if}}
      </nav>
    </header>
    <main class="container container-fluid mt-5">
      <!--cards for blog posts -->
      <div class="card" style="width: 18rem;">
  <div class="card-body">
    <h5 class="card-title">Post:
      {{post-name}}
    </h5>
    <p>Posted by {{post.user.name}} Posted on {{format_date post.date_created}}</p>
    <a href="#" class="card-link">Read Post</a>
  </div>
  <div class="card-body">
    <h5 class="card-title">Post:
      {{post-name}}
    </h5>
    <p>Posted by {{post.user.name}} Posted on {{format_date post.date_created}}</p>
    <a href="#" class="card-link">Read Post</a>
  </div>
  <div class="card-body">
    <h5 class="card-title">Post:
      {{post-name}}
    </h5>
    <p>Posted by {{post.user.name}} Posted on {{format_date post.date_created}}</p>
    <a href="#" class="card-link">Read Post</a>
  </div>
  </main>
     <!-- Render script for logged in users only -->
  {{#if logged_in}}
  <script src="/js/logout.js"></script>
  {{/if}}
</body>

</html>
