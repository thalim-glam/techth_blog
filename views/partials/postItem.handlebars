<div class="card partialView">
  <div class="card-header  flex-row justify-space-between">
    <h2>
      <a href="/post/{{id}}">
        <div class="col-8">{{blog.name}}</div>
      </a>
    </h2>
    <div class="col-3">posted by {{user.username}}
    </div>
  </div>  
  <p class="card-body">
   {{blog.description}}
  </p>
</div>